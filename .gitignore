node_modules
.next
out
next-env.d.ts
*.tsbuildinfo
.DS_Store
.env*
/scripts/.work
/verification
AGENTS.md
CLAUDE.md

# Generated by scripts/build-frames.mjs (see README). Never hand-edited, and
# rebuilt by `npm run build`, so they do not belong in version control.
/public/media/frames/

# NOTE: the hero SOURCE assets (hero-plate.* and source.mp4) are deliberately
# NOT ignored. They are the only inputs the frame builder cannot regenerate, so
# a deploy that lacks them has no hero at all. Commit them.
/lab
